# Copy to .env and fill values
PORT=3000
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
# Redirect URI configured in Google Cloud (e.g. http://localhost:3000/auth/callback). For mobile/Capacitor you can use a custom URI scheme like "gleyson://auth/callback" and then capture the code in-app.
# If using a custom scheme, set REDIRECT_URI here to the same value (and add it to Google Cloud allowed redirect URIs).
REDIRECT_URI=http://localhost:3000/auth/callback
# Optional: set a preferred Google Business Account ID to use
# BUSINESS_ACCOUNT_ID=

# Storage: by default server uses local encrypted-like storage (dev) via node-persist.
# In production, replace with secure vault/DB and remove tokens.json usage.
# TOKENS_KEY defines the key used for stored tokens (default: gleyson.tokens)
TOKENS_KEY=gleyson.tokens
# Refresh scheduler (minutes)
REFRESH_INTERVAL_MINUTES=60
# Refresh before expiry (minutes)
REFRESH_BEFORE_EXPIRY_MINUTES=5
